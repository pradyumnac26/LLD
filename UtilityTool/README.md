# UtilityTool

A small utility module with reusable number helper functions in Java.

## Files

- `src/NumberUtils.java`: Provides simple static methods for common numeric operations.

## Usage

Compile and run with:

```bash
javac UtilityTool/src/NumberUtils.java
java -cp UtilityTool/src NumberUtils
```
